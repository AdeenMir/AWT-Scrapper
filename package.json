{
  "name": "web-scrapper",
  "private": true,
  "scripts": {
    "start": "cd server && npm start",
    "build:local": "cd client && npm install --legacy-peer-deps && npm run build && node server/scripts/copy-public.js"
  }
}
